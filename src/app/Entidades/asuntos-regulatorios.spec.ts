import { AsuntosRegulatorios } from './asuntos-regulatorios';

describe('AsuntosRegulatorios', () => {
  it('should create an instance', () => {
    expect(new AsuntosRegulatorios()).toBeTruthy();
  });
});
