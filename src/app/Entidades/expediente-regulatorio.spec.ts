import { ExpedienteRegulatorio } from './expediente-regulatorio';

describe('ExpedienteRegulatorio', () => {
  it('should create an instance', () => {
    expect(new ExpedienteRegulatorio()).toBeTruthy();
  });
});
